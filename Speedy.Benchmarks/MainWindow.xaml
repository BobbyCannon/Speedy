<Window x:Class="Speed.Benchmarks.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:benchmarks="clr-namespace:Speed.Benchmarks"
		mc:Ignorable="d" Title="Speedy Benchmarks" Height="600" Width="750"
		WindowStartupLocation="CenterScreen" Icon="Speedy.ico"
		d:DataContext="{d:DesignInstance benchmarks:MainWindowModel}"
		Closing="MainWindowOnClosing">
	<Window.Resources>
		<Style x:Key="Button" TargetType="Button">
			<Setter Property="Padding" Value="10,4" />
			<Setter Property="Margin" Value="4" />
			<Setter Property="MinHeight" Value="26" />
			<Setter Property="MinWidth" Value="60" />
		</Style>
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0" Orientation="Horizontal">
			<Button Style="{StaticResource Button}" Content="{Binding SyncStatus}" Click="SyncOnClick" />
			<Button Style="{StaticResource Button}" Content="{Binding DatabaseStatus}" Click="DatabaseOnClick" />
			<Button Style="{StaticResource Button}" Content="{Binding RepositoryStatus}" Click="RepositoryOnClick" />
		</StackPanel>
		<ItemsControl Grid.Row="1" ItemsSource="{Binding SyncClients}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<WrapPanel Orientation="Horizontal" />
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<StackPanel Margin="10" Width="160">
						<TextBlock Text="{Binding Client.Name}" />
						<TextBlock Text="{Binding PreviousSyncedOn}" />
						<TextBlock Text="{Binding LastSyncedOn}" />
						<TextBlock Text="{Binding Status}" />
						<StackPanel Orientation="Horizontal">
							<TextBlock Margin="0,0,4,0">A: </TextBlock>
							<TextBlock Text="{Binding AddressCount}" />
						</StackPanel>
						<StackPanel Orientation="Horizontal">
							<TextBlock Margin="0,0,4,0">P: </TextBlock>
							<TextBlock Text="{Binding PeopleCount}" />
						</StackPanel>
					</StackPanel>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
		<ItemsControl Grid.Row="1" ItemsSource="{Binding DatabaseClients}">
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<WrapPanel Orientation="Horizontal" />
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<StackPanel Margin="10" Width="160">
						<TextBlock Text="{Binding Name}" />
						<TextBlock Text="{Binding LastProcessedOn}" />
						<StackPanel Orientation="Horizontal">
							<TextBlock Margin="0,0,4,0">A:</TextBlock>
							<TextBlock Text="{Binding AddressCount}" />
						</StackPanel>
						<StackPanel Orientation="Horizontal">
							<TextBlock Margin="0,0,4,0">P:</TextBlock>
							<TextBlock Text="{Binding PeopleCount}" />
						</StackPanel>
					</StackPanel>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
		<ScrollViewer Grid.Row="2" Height="100">
			<TextBox IsReadOnly="true" TextWrapping="Wrap" Text="{Binding Output}" />
		</ScrollViewer>
		<ProgressBar Grid.Row="3" Height="10" Value="{Binding Progress}" Minimum="0" Maximum="100" />
		<ScrollViewer Grid.Row="4">
			<TextBox IsReadOnly="true" TextWrapping="Wrap" Text="{Binding Errors}" />
		</ScrollViewer>
	</Grid>
</Window>